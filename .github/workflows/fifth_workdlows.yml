name: Fifth Workflows

on: [push, issues, pull_request]
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Events Vars
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
