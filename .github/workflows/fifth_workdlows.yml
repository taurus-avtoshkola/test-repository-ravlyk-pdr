name: Fifth Workflows

on:
  issues:
    types: [edited, deleted]
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Events Vars
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
